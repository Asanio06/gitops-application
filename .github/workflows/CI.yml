name: CI

on:
  push:
    branches:
      - main
      - develop

env:
  IMAGE_NAME: examplenode-dev
  DOCKER_HUB_USER: asanio06
  
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Login to DockerHub
      uses: docker/login-action@v1
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}
        
    - name: Build and push
      uses: docker/build-push-action@v2
      with:
        context: .
        push: true
        tags: ${{ env.DOCKER_HUB_USER }}/${{ env.IMAGE_NAME }}:${{ github.run_number }}

    - name: Repository Dispatch
      uses: peter-evans/repository-dispatch@v1
      with:
        token: ${{ secrets.ACTIONS_PAT }}
        repository: ${{ github.actor }}/gitops-config
        event-type: new-image
        client-payload: '{"image": "asanio06/${{ env.IMAGE_NAME }}:${{ github.run_number }}"}'